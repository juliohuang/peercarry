{
  "version": 1,
  "name": "ZCode empty input (manual execute only)",
  "target": { "exe": "ZCode.exe", "title": "ZCode" },
  "steps": [
    { "id": "focus", "action": "focus", "selector": { "role": "edit", "name": "向 ZCode 提问，使用 @ 添加上下文，使用 / 选择命令或能力" } },
    { "id": "input", "action": "input_text", "value": { "kind": "literal", "value": "peercarry 原生输入测试 ABC 123" } },
    { "id": "verify", "action": "assert", "assertion": { "kind": "text_equals", "selector": { "role": "edit", "name": "向 ZCode 提问，使用 @ 添加上下文，使用 / 选择命令或能力" }, "expected": "peercarry 原生输入测试 ABC 123" } }
  ]
}
